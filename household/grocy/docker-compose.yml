networks:
  web:
    external: true

services:
  # groceries
  grocy:
    image: linuxserver/grocy
    container_name: grocy
    networks:
      - web
    environment:
      PUID: ${PUID}
      PGID: ${PGID}
      TZ: ${TZ}
    volumes:
      - ${SSD_PATH}/grocy/config:/config
    # Not published: reached only via Caddy, which forward_auths to Authentik.
    expose:
      - "80"
    restart: unless-stopped
